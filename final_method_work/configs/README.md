# Config notes

This package uses script-generated YAML configs.  The main entry point is:

```bash
DEVICE=0 bash scripts/run_cadets_wra_rr_verdict.sh
```

Important WRA-RR override:

```bash
WRA_RR_WINDOW=11
```
